[Service]
# Fail closed before Docker starts, without a Requires= dependency that
# propagates a guard-service restart to production containers.
ExecStartPre=/usr/sbin/nft -f /etc/venmail-smtp-egress-guard.nft
